1. Select the `EasyResolutions_Settings` asset placed alongside this `Readme` file and click `Add to Preloaded Assets`.
2. Remove any scenes from the `Scene List` in `File -> Build Profiles`,
3. Add scenes from the `Scenes` folder of this sample to the `Scene List` in `File -> Build Profiles`, make sure they are properly ordered.
5. Go to `EasyResolutions_Settings` asset and make sure `Zero Scene` points to scene `01 -Resource Loading`, and the`Starting Scene (group)` is a group also pointing to `01 -Resource Loading`.